import React from "react";
import ReactDOM from "react-dom";

const App = () => {
    return (
        <div>
            <div>Döner</div>
        </div>
    );
};

ReactDOM.render(<App />,
    document.getElementById("root"));